name: Matrix Build

on: 
 workflow_dispatch:

jobs:
  matrix-job:
    strategy:
      matrix:
          os: [ubuntu-latest,windows-latest]
    runs-on:  ${{ matrix.os }}

    steps:
      - name: Show Runner
        run: echo "Running on $ {{ matrix.os }}"
